package com.skubit.dialog;

import com.gc.materialdesign.views.ButtonFloat;

import android.content.Context;
import android.util.AttributeSet;

public class FloatButton extends ButtonFloat {

    public FloatButton(Context context, final AttributeSet attrs) {
        super(context, attrs);
        setAttributes(attrs);
    }
}

